#include "_getline.h"

/**
 * strgetci - gets the index of the character.
 *
 * @s: pointer to a character in a string
 * @c: the character
 *
 * Return: the index of the character in the string.
 */
int strgetci(char *s, char c)
{
	int i;

	for (i = 0; s[i] != c; i++)
	{	
	}

	return (i);
}
